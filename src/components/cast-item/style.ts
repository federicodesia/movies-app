import styled from "styled-components"

export const ProfileImage = styled.img`
    flex-shrink: 0;
    
    display: block;
    height: 48px;
    width: 48px;
    object-fit: cover;
    border-radius: 50%;
`